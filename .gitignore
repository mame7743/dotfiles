# Machine-local overrides (never commit these)
*.local
.env
.env.*

# Multi-agent working state (may contain real IPs / internal topology — local only)
.multiagent/

# macOS
.DS_Store

# Editor
*.swp
*.swo

# Python
__pycache__/
*.pyc

# LLM skills — runtime data (never commit)
skills/*/db/*.db
skills/*/db/*.db-shm
skills/*/db/*.db-wal
skills/*/db/config.yaml
skills/*/run/
