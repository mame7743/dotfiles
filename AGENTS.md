# dotfiles — エージェント向け指示

## プロジェクト概要

個人用設定・運用メモリを管理するリポジトリ。Proxmox/Ansible のバックアップ設定は `/Users/atsushi/ansible` の別リポジトリで管理し、このリポジトリには作業状態と判断記録を保存する。

## コマンド

- build: —
- test: —
- lint: —
- dev: —

## 規約

- 秘密値、実 IP、トークン、パスワードを記録しない。
- Ansible の実装変更は `/Users/atsushi/ansible` 側で行う。
- 作業開始前に `docs/memory/README.md` の索引を読む。

## メモリ

このプロジェクトは永続メモリバンクを保持している。
重要な決定をしたら `docs/memory/YYYY-MM-DD-<slug>.md` に記録すること。

## 開発ワークフロー

自明でない変更では:

1. 要件と受け入れ条件を確認する。
2. 簡潔な実装計画を作る。
3. 最小限の一貫した変更を実装する。
4. まず対象を絞ったテストを実行する。
5. 必要に応じて広い回帰テストを実行する。
6. `verifier` に実装と要件の突き合わせを依頼する。
7. ドメイン規則やデータ意味論が変わる場合は `domain-reviewer` に依頼する。
8. 明示的な受け入れ判定が必要な場合は `judge` に依頼する。

些末な編集で全エージェントを起動しないこと。

## モデルの役割

- `build`: 通常の対話的開発。
- `plan`: 通常の実装計画。
- `planner`: アーキテクチャと高リスク変更。
- `implementer`: 承認済み計画からの実装。
- `verifier`: テスト・差分・回帰・要件カバレッジ。
- `domain-reviewer`: ドメイン意味論と業務規則のレビュー。
- `judge`: 必要に応じてJevを使う独立した受け入れ評価。

## ハード制約

決定論的チェックはモデルの判断より優先する。

- コンパイラ・lint・型チェック・テストの失敗は客観的な失敗である。
- 固定要件は直接チェックする。
- セキュリティと認可の変更は明示的な検証を要する。
- スキーマ変更はマイグレーションとロールバックを検討する。
- 生成物は、生成が成功したという理由だけで検証済みとみなさない。

Jev や LLM の結論が、失敗した決定論的チェックを覆してはならない。

## Jev 判定プロトコル

Jev は、証拠を集めた後の閉じた明確な意思決定にのみ使う（`jev_yesno` /
`jev_choice` / `jev_score`）。Jev に渡す state は:

- 議論ではなく事実を含む;
- 欠落した証拠を明示する;
- テスト・検証結果を含む;
- 簡潔に保つ;
- 秘密値や機密の生データを含めない。

判定:

- `approve`: 全てのハードゲートを通過し、証拠が受け入れを支持する;
- `revise`: 方針は妥当だが不足が残る;
- `reject`: 要件・安全性・アーキテクチャに根本的な問題がある。

導入初期は Jev は助言である。Jev の判定と最終的な人間の判断の両方を記録し、
不一致を後から評価できるようにする。

## 完了報告

1. 何を変更したか。
2. 何を検証したか。
3. 実行したコマンドとテスト。
4. 残るリスクや前提。
5. 該当する場合はドメインレビュー結果。
6. Jev を使った場合は入力の要約と結果。
7. 人間の承認がまだ必要か。
